<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hacker Terminal</title>
    <!-- Tailwind CSS CDN for basic utilities -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom CSS for the CRT effect */
        body {
            background-color: #000000; /* Black background */
            color: #00FF00; /* Bright green font */
            font-family: 'Courier New', Courier, monospace; /* Monospace font for terminal feel */
            padding: 2rem; /* Add some padding around the content */
            line-height: 1.5; /* Improve readability */
            overflow-x: hidden; /* Prevent horizontal scroll */
        }

        .container {
            max-width: 800px; /* Limit content width */
            margin: 0 auto; /* Center the container */
            border: 1px solid #00FF00; /* Green border for a framed look */
            padding: 1.5rem;
            box-shadow: 0 0 15px #00FF00; /* Green glow effect */
            border-radius: 0.5rem; /* Slightly rounded corners */
        }

        pre {
            white-space: pre-wrap; /* Preserve whitespace and wrap long lines */
            word-wrap: break-word; /* Break words if necessary */
            margin-top: 2rem;
            margin-bottom: 2rem;
            text-align: center; /* Center the ASCII art */
        }

        a {
            color: #33FF33; /* Slightly different green for links to stand out */
            text-decoration: underline; /* Underline links */
            transition: color 0.3s ease; /* Smooth transition for hover effect */
        }

        a:hover {
            color: #66FF66; /* Lighter green on hover */
        }

        /* Responsive adjustments */
        @media (max-width: 640px) {
            body {
                padding: 1rem;
            }
            .container {
                padding: 1rem;
                border-radius: 0.25rem;
            }
            pre {
                font-size: 0.8em; /* Smaller font for ASCII art on small screens */
            }
        }
    </style>
</head>
<body class="flex flex-col items-center justify-center min-h-screen">
    <div class="container">
        <!-- The quote -->
        <p class="text-lg mb-4">
            We are hackers, and hackers have black terminals with green text.
        </p>

        <!-- ASCII Penguin Art -->
        <pre>
           _
          /_\
         (_o_)
        /|   |\
       / |   | \
      /  |   |  \
     /   |   |   \
    /____|___|____\
        </pre>

        <!-- The link -->
        <p class="text-center">
            <a href="https://joe0121-rnwox.wordpress.com/" target="_blank" rel="noopener noreferrer">
                Visit the original source
            </a>
        </p>
    </div>
</body>
</html>
